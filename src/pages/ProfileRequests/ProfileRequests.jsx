import React from 'react';
import PropTypes from 'prop-types';

const RequestsUserPage = props => {
    return (
        <div>

        </div>
    );
};

RequestsUserPage.propTypes = {
    
};

export default RequestsUserPage;